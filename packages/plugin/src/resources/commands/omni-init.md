---
description: Bootstrap OmniCode durable memory for this project
agent: omnicode
---
Bootstrap the current project for OmniCode.

Requirements:
- call `omnicode_bootstrap`
- inspect the current project briefly
- update `.omni/PROJECT.md` and `.omni/STATE.md` with the current context
- append a concise bootstrap note to `.omni/SESSION-SUMMARY.md`
- do not implement product changes yet unless the user explicitly asked for implementation in the same request
