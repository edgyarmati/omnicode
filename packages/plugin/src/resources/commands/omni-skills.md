---
description: Suggest and sync bundled OmniCode skills for the current work
agent: omnicode
---
Interpret the current work from `$ARGUMENTS`.

Then:
- call `omnicode_suggest_skills`
- call `omnicode_update_skills`
- summarize which bundled skills should be used next and why
