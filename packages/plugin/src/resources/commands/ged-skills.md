---
description: Suggest and sync bundled GedCode skills for the current work
agent: gedcode
---
Interpret the current work from `$ARGUMENTS`.

Then:
- call `gedcode_suggest_skills`
- call `gedcode_update_skills`
- summarize which bundled skills should be used next and why
